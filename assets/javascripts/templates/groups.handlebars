<div class='six columns'>
  <div class='groups'>
    {{#each group in controller}}
      <a {{action showGroup group href=true}}>{{group.full_name}}</a>
    {{/each}}
  </div>
</div>
<div class="six columns">
  {{outlet}}
</div>